<Window x:Class="PL.DronesListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="DisplaydronesWindow" Height="450" Width="800"
        Icon="DroneIcon.png">
    <Grid x:Name="mainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="400*"/>
        </Grid.RowDefinitions>
        <Grid Name="UpGrid" HorizontalAlignment="Stretch"
               Height="auto" Grid.Row="0" VerticalAlignment="Stretch" Width="auto">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions >
            <ComboBox x:Name="comboStatus" Grid.Column="1" Margin="5" 
                      IsEditable="true"  IsReadOnly="true"  
                      HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                      SelectionChanged="comboStatus_SelectionChanged" />
            <TextBox x:Name="txtTemp"  Margin="5" Text="Drone Status:" TextWrapping="Wrap" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" IsReadOnly="True"/>
            <TextBox x:Name="btnAddDrone" Text="Drone max weight:" Grid.Column="2" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" Margin="5" IsReadOnly="True"/>
            <ComboBox x:Name="comboMaxWeight" Grid.Column="3" Margin="5" 
                IsEditable="true"  IsReadOnly="true"
                SelectionChanged="comboMaxWeight_SelectionChanged" />
        </Grid>
        <ListView x:Name="lstvDrones" Margin="5,48,5,72" MouseDoubleClick="openActionsWindow" SelectionChanged="lstvDrones_SelectionChanged" Grid.RowSpan="2" />
        <Button x:Name="btnAddDrones" Content="add Drone" HorizontalAlignment="Left" Margin="520,314,0,0" Grid.Row="1" VerticalAlignment="Top" Height="36" Width="142" Click="btnAddDrones_Click"/>
    </Grid>
</Window>
